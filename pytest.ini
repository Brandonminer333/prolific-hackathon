[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: isolated tests that exercise a single function without project dependencies
    functional: tests that compose imported project functions or modules
    integration: tests that exercise running application features such as the API
addopts = -ra
