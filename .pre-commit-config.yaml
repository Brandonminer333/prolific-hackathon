repos:
  - repo: local
    hooks:
      - id: pytest-unit-functional
        name: pytest unit and functional tests
        entry: bash -c 'cd "$(git rev-parse --show-toplevel)" && .venv/bin/pytest -m "unit or functional"'
        language: system
        pass_filenames: false
        always_run: true
